# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files - NEVER commit sensitive data
.env
.env.local
.env.production
.env.*.local
backend/.env
backend/.env.local
backend/.env.production

# Sensitive files - API keys, secrets, credentials
APIS.txt
encryption_key.txt
secrets_production.txt
*_key.txt
*_secret.txt
*.pem
*.key

# Temporary files
temp_*.sql
temp_*.csv
temp_*.txt
test_output*.txt
*.tmp

# Generated documents (never commit binary outputs)
*.pdf
*.xlsx
*.xls
tsc_output.txt

# Deploy packages (regenerated by prepare_deploy.ps1)
deploy_package/
deploy_*.zip

# Archives - Old/obsolete files (do not include in build or commits)
Archives/

# Database backups (contain production data)
backups/
*.sql.gz

# Mobile app — dépôt git indépendant, ne pas inclure ici
trackyu-mobile-expo/

# Snapshot VPS récupéré (backup ponctuel, pas du source)
recovered_vps_2026-04-03/

# Monitoring — données runtime Prometheus/Grafana/Alertmanager
monitoring/data/
monitoring/grafana/data/

# Docker env — contient des secrets de production
.env.docker
.env.staging

# Artefact Vite (généré automatiquement)
__vite-browser-external

# Build cache
.vite/
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
