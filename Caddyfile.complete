sms.abidjangps.com {
    reverse_proxy app:3000
}

trackyugps.com, www.trackyugps.com {
    # Security headers (production)
    header {
        # Anti-clickjacking
        X-Frame-Options "DENY"

        # Content Security Policy complète
        Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: blob: https:; connect-src 'self' https://trackyugps.com https://live.trackyugps.com wss://trackyugps.com wss://live.trackyugps.com; media-src 'self' blob:; worker-src blob:; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'"

        # HSTS — force HTTPS pour 1 an, éligible au preload list
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

        # Empêche le MIME-sniffing
        X-Content-Type-Options "nosniff"

        # Referrer limité à l'origine
        Referrer-Policy "strict-origin-when-cross-origin"

        # Désactiver les APIs navigateur non utilisées
        Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=()"

        # Supprimer X-Powered-By si présent côté backend
        -X-Powered-By
    }

    # API requests -> backend (port 3001)
    # Rate limiting géré par express-rate-limit dans server.ts :
    #   - Global   : 1000 req/15min par IP
    #   - /auth/login : 5 req/heure par IP (anti-brute-force)
    handle /api* {
        reverse_proxy 172.17.0.1:3001
    }

    # Socket.IO -> backend (with WebSocket support)
    handle /socket.io* {
        reverse_proxy 172.17.0.1:3001 {
            header_up Upgrade {http.request.header.Upgrade}
            header_up Connection {http.request.header.Connection}
        }
    }

    # Everything else -> frontend nginx
    handle {
        reverse_proxy 172.17.0.1:8080
    }
}

staging.trackyugps.com {
    # Logs JSON
    log {
        output file /var/log/caddy/staging-access.log
        format json
    }

    # Security headers (staging — identiques à prod)
    header {
        X-Frame-Options "DENY"
        Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: blob: https:; connect-src 'self' https://trackyugps.com https://staging.trackyugps.com https://live.trackyugps.com wss://trackyugps.com wss://staging.trackyugps.com wss://live.trackyugps.com; media-src 'self' blob:; worker-src blob:; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'"
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=()"
        X-Environment "staging"
        -X-Powered-By
    }

    # API requests -> backend staging (port 3002)
    handle /api* {
        reverse_proxy 172.17.0.1:3002
    }

    # Socket.IO -> backend staging (with WebSocket support)
    handle /socket.io* {
        reverse_proxy 172.17.0.1:3002 {
            header_up Upgrade {http.request.header.Upgrade}
            header_up Connection {http.request.header.Connection}
        }
    }

    # Everything else -> frontend staging nginx (port 8081)
    handle {
        reverse_proxy 172.17.0.1:8081
    }
}
